<div class="row">
  <div class="well">
    <h2>All notes</h2>
      <% @notes.each do |note| %>
        <p><%= note.title %></p>
        <p><%= note.content %></p>
        <p><%= time_ago_in_words(note.created_at) %></p>
      <% end %>
  </div>
</div>

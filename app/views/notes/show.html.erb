<div class="row">

<p><%= @note.title %></p>
<p><%= @note.content %></p>
<button class="btn btn-primary"><%= link_to "Edit Note", edit_note_path(@note) %></button>
<button class="btn btn-primary"><%= link_to "Delete Note", note_path(@note),
                                  method: :delete, data: {confirm: "are you sure?"} %></button>
</div>

<p><%= link_to "All Notes", notes_path %></p>
